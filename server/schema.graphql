type User {
  id: ID!
  email: String!
  isPremium: Boolean!
}

type AuthPayload {
  token: String!
  user: User!
}

type Query {
  ping: String!
}

type Mutation {
  signup(email: String!, password: String!): AuthPayload!
  login(email: String!, password: String!): AuthPayload!
}
